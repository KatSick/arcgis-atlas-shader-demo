# ArcGIS Dictionary renderer for APP6-D

- ArcGIS with custom WebGL layer which renders APP6D symbols as Raster images directly on GPU.
- Used milsymbol preprocessing to form app6d.png sprite + app6d.json mapping
- Fastest rendering.= of symbols. Hard to add apmlifiers

## Run

```bash
bun dev:arcgis-gpu-direct
```
